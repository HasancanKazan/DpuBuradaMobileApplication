<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hasancankazan.com.dpuburadaprojesi">

    <application
        android:allowBackup="true"
        android:icon="@drawable/uygulamafotosu"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".anasayfa"
            android:label="@string/app_name"
            android:screenOrientation="portrait">
        </activity>

        <activity android:name=".yemek"
            android:label="@string/app_name">
        </activity>

        <activity android:name=".yemeklistesi"
            android:theme="@style/AppTheme.CustomTheme">
        </activity>

        <activity android:name=".ulasim"
            android:theme="@string/app_name">
        </activity>

        <activity android:name=".etkinlik"
            android:label="@string/app_name">
        </activity>

        <activity android:name=".hesap"
            android:label="@string/app_name"
            android:screenOrientation="portrait">
        </activity>


        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>

            </intent-filter>
        </activity>


    </application>

</manifest>
